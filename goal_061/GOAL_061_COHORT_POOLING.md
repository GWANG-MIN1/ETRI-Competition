# GOAL_061 — HS-JEPA cohort pooling (representation-similarity per-subject LEVEL pooling) = FAIL

HS-JEPA subject embeddings near-degenerate (pairwise cosine ~0.98) -> no exploitable cohort
structure. Pooling does not beat own/global/placebo on forward-CV future, transfer-gate PASS_LEVEL
NONE. The world-model z encodes within-subject circadian dynamics, not between-subject traits.
anchor 0.5615 retained.
